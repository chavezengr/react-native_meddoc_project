import { createSlice } from '@reduxjs/toolkit'
import { initialState } from './model'

export const md0000Slice = createSlice({
  name: 'md0000',
  initialState,
  reducers: {},
})

export const {} = md0000Slice.actions

export default md0000Slice.reducer
