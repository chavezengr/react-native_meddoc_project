login expo:
    npx expo login
register expo:
    npx expo register
customize expo:
    npx expo customize
current expo:
    npx expo whoami
doctor:
    npx react-native doctor

start expo:
    npx expo start


    npx react-native clean
    npx react-native start --reset-cache
    npm cache clean --force

    npm start -- --reset-cache

resolve development build package:
    npx expo run:android

npm install:
npm install --ignore-existing


rm -rf node_modules
npm cache clean --force
npm install

cd android
./gradlew clean

npx react-native start --reset-cache
npx react-native run-android